import { StyleSheet, Text, View } from "react-native";
import React from "react";
import AcquireDetail from "@/features/acquire/components/acquire-detail";

const AcquireDetailPage = () => {
  return <AcquireDetail />;
};

export default AcquireDetailPage;

const styles = StyleSheet.create({});
