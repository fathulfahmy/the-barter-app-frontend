import { StyleSheet, Text, View } from "react-native";
import React from "react";
import CreateAcquire from "@/features/acquire/components/create-acquire";

const CreateAcquirePage = () => {
  return <CreateAcquire />;
};

export default CreateAcquirePage;

const styles = StyleSheet.create({});
